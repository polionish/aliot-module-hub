/**
 * Конфигурация приложения.
 * @type {Object}
 * @property {number} ANIMATION_DURATION - Длительность анимаций (мс).
 * @property {number} SCROLL_THRESHOLD - Порог прокрутки для скрытия модулей (пиксели).
 * @property {number} MODULE_FADE_DURATION - Длительность анимации появления/скрытия модулей (мс).
 */
export const CONFIG = {
    ANIMATION_DURATION: 600,
    SCROLL_THRESHOLD: 100,
    MODULE_FADE_DURATION: 500
};